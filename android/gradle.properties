org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# AGP 9 enables strict namespace uniqueness by default. LiteRT 2.2.0 currently ships
# litert and litert-api with the same namespace, so keep the compatibility flag until
# the upstream artifacts use distinct namespaces.
android.uniquePackageNames=false
kotlin.code.style=official
